# Repository Rules

## Git workflow

- Use the `dev` branch for all work in this repository.
- Do not create or switch to task, feature, fix, or `codex/*` branches.
- Commit and push changes directly to `origin/dev` unless the user explicitly requests otherwise.
